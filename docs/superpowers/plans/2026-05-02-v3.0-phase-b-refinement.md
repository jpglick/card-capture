# v3.0 Phase B: Refinement Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement Stage 4 (Precision Normalization) and Stage 5 (Visual Deduplication) of the v3.0 Adaptive Pipeline.

**Architecture:** A functional approach where normalization and deduplication are implemented as standalone components in the "Refinery" phase. Standardizes all card views to a uniform $750 \times 1050$ resolution and uses perceptual hashing to flag physical duplicates.

**Tech Stack:** Python, OpenCV, NumPy, SQLite.

---

### Task 1: Precision Normalizer (Stage 4)

**Files:**
- Modify: `src/card_capture/cropper.py`
- Test: `tests/test_cropper.py`

- [ ] **Step 1: Implement PrecisionNormalizer in cropper.py**

Add a class that standardizes card crops to $750 \times 1050$ with a $1.5\%$ safety inner crop.

```python
class PrecisionNormalizer:
    def __init__(self, width: int = 750, height: int = 1050, safety_margin: float = 0.015):
        self.width = width
        self.height = height
        self.safety_margin = safety_margin

    def normalize(self, image: np.ndarray, corners: Sequence[Point]) -> np.ndarray:
        # 1. Order corners clockwise
        # 2. Compute perspective transform to self.width x self.height
        # 3. Warp using cv2.INTER_LANCZOS4
        # 4. Apply safety inner crop (remove background bleed)
        # 5. Resize back to self.width x self.height
        pass
```

- [ ] **Step 2: Write test for PrecisionNormalizer**

```python
def test_precision_normalization_output_size():
    normalizer = PrecisionNormalizer()
    image = np.zeros((2000, 2000, 3), dtype=np.uint8)
    corners = [(100, 100), (1100, 100), (1100, 1600), (100, 1600)]
    result = normalizer.normalize(image, corners)
    assert result.shape == (1050, 750, 3)
```

- [ ] **Step 3: Implement minimal PrecisionNormalizer**

- [ ] **Step 4: Run tests**

Run: `pytest tests/test_cropper.py`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/card_capture/cropper.py tests/test_cropper.py
git commit -m "feat: implement precision normalization with safety crop"
```

---

### Task 2: Visual Deduplicator (Stage 5)

**Files:**
- Create: `src/card_capture/deduplicator.py`
- Test: `tests/test_deduplicator.py`

- [ ] **Step 1: Implement VisualDeduplicator**

Use pHash on the inner $60\%$ of the card image.

```python
class VisualDeduplicator:
    def __init__(self, threshold: int = 4):
        self.threshold = threshold

    def compute_phash(self, image: np.ndarray) -> str:
        # 1. Crop to inner 60% (artwork)
        # 2. Convert to grayscale, resize to 32x32
        # 3. Compute DCT and get pHash bitstring
        pass

    def is_duplicate(self, hash1: str, hash2: str) -> bool:
        # Hamming distance < threshold
        pass
```

- [ ] **Step 2: Write tests for VisualDeduplicator**

```python
def test_deduplicator_detects_same_card():
    # Mock two images of the same card with slight noise
    # Assert is_duplicate is True
```

- [ ] **Step 3: Implement minimal VisualDeduplicator**

- [ ] **Step 4: Run tests**

- [ ] **Step 5: Commit**

---

### Task 3: Storage Updates for Deduplication Results

**Files:**
- Modify: `src/card_capture/storage.py`

- [ ] **Step 1: Update card_instances table**

Add `visual_hash` and `is_duplicate_of` (linking to another instance_id) columns.

- [ ] **Step 2: Add update_instance_hash method**

```python
def update_instance_hash(self, instance_id: int, visual_hash: str, duplicate_of_id: Optional[int] = None):
    pass
```

- [ ] **Step 3: Commit**

---

### Task 4: Pipeline Integration (Refinery Phase)

**Files:**
- Modify: `src/card_capture/pipeline.py`

- [ ] **Step 1: Update VideoProcessor.process**

Integrate `PrecisionNormalizer` and `VisualDeduplicator` after tracking but before final persistence.

- [ ] **Step 2: Save normalized images**

Save the high-quality normalized crops for use in Stage 6 (Fusion).

- [ ] **Step 3: Run integration tests**

- [ ] **Step 4: Commit**
