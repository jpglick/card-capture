# v3.0 Phase C: Fusion & UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement Stage 6 (Lighting-Diverse Fusion) and Stage 7 (Metadata & UI Refactor) of the v3.0 Adaptive Pipeline.

**Architecture:** A multi-frame fusion engine that selects lighting-diverse views from a track and blends them to eliminate glare. The UI is refactored to prioritize these "perfect" fused images while allowing inspection of source evidence.

**Tech Stack:** Python, OpenCV, NumPy, FastAPI, Jinja2.

---

### Task 1: Lighting-Diverse Multi-Frame Fuser (Stage 6)

**Files:**
- Create: `src/card_capture/fuser.py`
- Create: `tests/test_fuser.py`

- [ ] **Step 1: Implement Glare Centroid logic**

```python
def find_glare_centroid(image: np.ndarray) -> Optional[Tuple[float, float]]:
    # 1. Convert to grayscale
    # 2. Find brightest 5% of pixels
    # 3. Return (x, y) centroid of these pixels
    pass
```

- [ ] **Step 2: Implement MultiFrameFuser class**

```python
class MultiFrameFuser:
    def fuse(self, normalized_images: List[np.ndarray]) -> np.ndarray:
        # 1. Map glare centroids for all frames
        # 2. Partition 750x1050 into 4 quadrants
        # 3. For each quadrant, find the frame with glare centered in it
        # 4. If multiple, pick the sharpest
        # 5. np.median blend the selected 3-4 frames
        pass
```

- [ ] **Step 3: Write tests for MultiFrameFuser**

```python
def test_fusion_erases_simulated_glare():
    # 1. Create 4 identical base images
    # 2. Add white "glare" circles in different quadrants to each
    # 3. Assert fused image median eliminates the white circles
```

- [ ] **Step 4: Commit**

```bash
git add src/card_capture/fuser.py tests/test_fuser.py
git commit -m "feat: implement multi-frame glare-eliminating fusion"
```

---

### Task 2: Pipeline & Storage Integration (Stage 6 + 7)

**Files:**
- Modify: `src/card_capture/storage.py`
- Modify: `src/card_capture/pipeline.py`
- Modify: `src/card_capture/models.py`

- [ ] **Step 1: Update card_instances table**

Add `fused_image_path` (TEXT) column.

- [ ] **Step 2: Update add_card_view and view storage**

Add `glare_x`, `glare_y`, and `sharpness` columns to `card_views`.

- [ ] **Step 3: Integrate Fuser into VideoProcessor.process**

After normalizer loop in Phase B, use `MultiFrameFuser` on the collected `normalized` images. Save to `{output_dir}/best/instance_{id}_fused.jpg`.

- [ ] **Step 4: Update saved_cards logic**

Update legacy `saved_cards` table to point to the `fused_canonical.jpg` where available.

- [ ] **Step 5: Run integration tests**

- [ ] **Step 6: Commit**

---

### Task 3: UI Refactor (Stage 7)

**Files:**
- Modify: `src/card_capture/review.py`
- Modify: `src/card_capture/templates/review.html`

- [ ] **Step 1: Update review.py API**

Update `index()` to retrieve angle tags and fusion paths. Add endpoint to serve fused images if different from legacy `image()` endpoint.

- [ ] **Step 2: Refactor review.html Template**

- [ ] **Step 3: Add "Angle" indicator (Front/Back badge)**

- [ ] **Step 4: Add Source Carousel**

Add a small horizontal scroll area below the main image showing the raw `normalized` views that built the fusion.

- [ ] **Step 5: Verify UI manually**

- [ ] **Step 6: Commit**
