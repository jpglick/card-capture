# Implementation Plan: Task 3.2 (Modify `review.html`)

## Implementation
1. Add angle indicator badge near the card.
2. Update logic to use `fused_image_path` (or `card.fused_image_path`) if available.
3. Add a link/section for "Source Frames".

## Verification
- Run review app and check visual UI improvements.
