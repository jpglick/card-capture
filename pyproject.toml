[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "card-capture"
version = "0.1.0"
description = "Extract high-quality sports card stills from local video files."
requires-python = ">=3.9"
dependencies = [
  "numpy",
  "opencv-python",
  "Pillow>=10.4.0",
  "supervision>=0.21,<0.28",
  "torch>=2.6.0",
  "torchvision",
]

[project.optional-dependencies]
pipeline_v21 = [
  "av",
  "onnxruntime",
  "boxmot>=0.14.0",
]
model = [
  "huggingface-hub",
  "ultralytics",
]
review = [
  "fastapi",
  "jinja2",
  "python-multipart",
  "uvicorn",
]
test = [
  "pytest",
  "httpx",
  "fastapi",
  "pydantic>=2",
]
harness = [
  "pydantic>=2",
]

[project.scripts]
card-capture = "card_capture.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src", "."]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
card_capture = ["templates/*.html"]
