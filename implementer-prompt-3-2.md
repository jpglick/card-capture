# Implementer Prompt: Task 3.2 (Modify `review.html`)

## Objective
Modify `src/card_capture/templates/review.html` to support the v3 UI features.

## Requirements
1. Add a badge/indicator for the `angle` (Front/Back) near the card.
2. Update the card display to use the `fused_image_path` (if available) as the primary image, otherwise fall back to the existing image source.
3. Add a section or link for "Source Frames".

## Verification
- Run review app and check visual UI improvements.
